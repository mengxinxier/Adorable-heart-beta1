# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

MGU_unit_production = {
    enable = {
        original_tag = MGU
       
    }
    enable = {
        has_government = communism
        date < 1940.1.1
    }
    abort = {
        
    }
    ai_strategy = {
        type = role_ratio	#模板倾向
        id = MGU_infantry
        value = 1000
    }
    ai_strategy = {
        type = template_prio	#训练模板倾向
        id = MGU_infantry
        value = 1000
    }
    ai_strategy = {
        type = role_ratio	#模板倾向
        id = MGU_armor
        value = 0
    }
    ai_strategy = {
        type = template_prio	#训练模板倾向
        id = MGU_armor
        value = 0
    }
}

MGU_unit_production_2 = {
    allowed = {
        original_tag = MGU
    }
    enable = {        
        has_government = communism    
        OR = {
			has_tech = basic_medium_tank
			has_tech = basic_medium_tank_chassis
		}  
    }
    abort = {
        
    }
    ai_strategy = {
        type = role_ratio	#模板倾向
        id = MGU_infantry
        value = 10
    }
    ai_strategy = {
        type = template_prio	#训练模板倾向
        id = MGU_infantry
        value = 10
    }
    ai_strategy = {
        type = role_ratio	#模板倾向
        id = MGU_armor
        value = 1000
    }
    ai_strategy = {
        type = template_prio	#训练模板倾向
        id = MGU_armor
        value = 1000
    }
}

MGU_equipment_production = { #默认装备
    allowed = {
        original_tag = MGU
    }
    enable = {
       has_government = communism
    }
    ai_strategy = {
        type = equipment_production_factor	#装备倾向
        id = infantry_weapons
        value = 40
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = artillery
        value = 3000
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = armor
        value = 2000
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = construction_tech
        value = 200
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = industry
        value = 1000
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = electronics
        value = 200
    }
}

MGU_equipment_production_2 = { #默认装备
    allowed = {
        original_tag = MGU
    }
    enable = {
         OR = {
			has_tech = basic_medium_tank
			has_tech = basic_medium_tank_chassis
		}      
        has_government = communism
    }
    ai_strategy = {
        type = equipment_production_min_factories
        id = land_medium_tank
        value = 10
    }
}

MGU_wants_civ = {
	allowed = {
		original_tag = MGU
	}
	enable = {
		date < 1937.6.1
		has_war = no
        has_government = communism
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = building_target
		id = industrial_complex
		value = 10000
	}
}

MGU_prio_military_even_at_peace = {
    allowed = {
		original_tag = MGU
	}
	enable = {
		date > 1937.6.1
		date < 1942.1.1
        has_government = communism
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = building_target
		id = arms_factory
		value = 10000
	}
}

MGU_no_wants_dockyard  = { #别造船厂
    allowed = {
        original_tag = MGU
    }
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = building_target 
        id = dockyard
        value = -999
    }
}

MGU_naval_role_ratios_historical = { #海军
    allowed = {
        has_dlc = "Man the Guns"
        original_tag = MGU
        has_government = communism
    }

    enable = {
        always = yes
    }

    ai_strategy = {
        type = role_ratio
        id = naval_capital_bb
        value = -25
    }

    ai_strategy = {
        type = role_ratio
        id = naval_capital_bc
        value = -100
    }

    ai_strategy = {
        type = role_ratio
        id = naval_carrier
        value = -50
    }

    ai_strategy = {
        type = role_ratio
        id = naval_submarine
        value = -30
    }

    ai_strategy = {
        type = role_ratio
        id = naval_screen
        value = -120
    }

    ai_strategy = {
        type = role_ratio
        id = naval_cruiser_light
        value = -15
    }

    ai_strategy = {
        type = role_ratio
        id = naval_cruiser_heavy
        value = -50
    }

    ai_strategy = {
        type = role_ratio
        id = naval_mine_sweeper
        value = -15
    }

    ai_strategy = {
        type = role_ratio
        id = naval_mine_layer
        value = -80
    }
}

MGU_stop_disbanding_your_army = {#ai部署陆军倾向
    allowed = {
        original_tag = MGU
        has_government = communism
    }
    enable = {
        num_divisions < 500
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 1000
    }
    ai_strategy = {
        type = area_priority
        id = south_america
        value = 1000
    }
	ai_strategy = {
        type = force_build_armies
        value = 100
    }



}
MGU_stop_disbanding_your_army2 = {#ai部署陆军倾向2
    allowed = {
        original_tag = MGU
        has_government = communism
    }
    enable = {
        num_divisions > 500
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = -2000
    }
}

MGU_home_land_is_safe = { #不驻扎
    allowed = {
        original_tag = MGU
        has_government = communism
    }

    enable = {
        always = yes
    }

    ai_strategy = { #驻军
        id = garrison
        value = 0
    }

}

LHS_unit_buffer_for_asian_threat = {#退守战场
    allowed = {
        original_tag = MGU
    }
    enable = {
        has_government = communism
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = put_unit_buffers

        # ratio of total armeis in country to be buffered

        # you can specify an order id. ratio of same orders ids will be share same ratio
        #order_id = 2

        # states to put garrison orders (if no state is friendly, strat is invalid)
        states = {
            501
            500
            503
            894
        }
        ratio = 0.2
        area = europe
        area = asia
        area = africa
        area = south_america
        area = north_america
        area = neighbor
        area = faction
        # ai areas that the orders will use these buffers in
        # by default if you have orders in target areas, the buffer will request less units.
        # you can override this to disable the feature
        subtract_invasions_from_need = no
        subtract_fronts_from_need = no
    }
}


MGU_rush_all = {
    allowed = {
        original_tag = MGU
        has_government = communism
    }

    enable = {
        has_war = yes
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = PAO
        value = 20 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = PAQ
        value = 100 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = LFC
        value = 20 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = ELF
        value = 20 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = EWW
        value = 20 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_control
        tag = PAQ
        ratio > 0.10
        priority = 100
        ordertype = front
        execution_type = rush
        manual_attack = no
    }
    ai_strategy = {
        type = front_control
        tag = LFC
        ratio > 0.10
        priority = 50
        ordertype = front
        execution_type = rush
        manual_attack = no
    }
}

MGU_AI_brain_up = {
    allowed = {
        original_tag = MGU
        has_government = communism
    }
    enable = {
        has_war = yes
    }
    ai_strategy = {
	    type = naval_invasion_dominance_weight  # 类型=海军入侵制海权重
	    value = 0  # 值=30
    }
    ai_strategy = {
	    type = force_defend_ally_borders  # 类型=不防守盟国边境
	    id = LHS  # ID=意大利
	    value = 100  # 该策略为二元制，值>0时激活，≤0时停用。
    }


}
MGU_AI_brain_up_2 = {
    allowed = {
        original_tag = MGU
        has_government = communism
    }
    enable = {
        date > 1939.1.1
    }
    ai_strategy = {
	    type = invade  # 类型=入侵
	    id = PAQ # ID=意大利
	    value = 100  # 负值，AI将避免入侵该国家
	    # value = 60  # 正值，若为潜在敌人，针对该国家的入侵重要性得分将乘以1.6
    }  
    ai_strategy = {
        type = prepare_for_war
        id = PAQ
        value = 999
    }     
    ai_strategy = {
	    type = force_concentration_target_weight  # 类型=兵力集中目标权重
	    tag = PAQ						# 目标特定国家，可指定多个
	    #state = 42							# 目标省份，可指定多个
	    #strategic_region = 65				# 目标战略区域，可指定多个
	    country_trigger = { PAQ }	# 针对特定国家的触发条件，作用域为敌国，FROM作用域为我方国家
	    #state_trigger = { always = no }		# 针对省份的触发条件，作用域为省份，FROM作用域为敌国，FROM.FROM作用域为我方国家
	    value = 100						# 常规优先级系数，40表示+40%，-60表示-60%
    }
    ai_strategy = {
	    # 二选一
	    type = front_unit_request  # 类型=战线部队请求/入侵部队请求
	    # 入侵指令检查入侵目标，战线指令检查战线省份
	    tag = PAQ							# 目标特定国家，可指定多个
	    state = { 1199 896 1200 1201 1202 1191 1192 }					# 目标省份，可指定多个
	    #strategic_region = 65				# 目标战略区域，可指定多个
	    country_trigger = { PAQ }	# 针对特定国家的触发条件，作用域为敌国，FROM作用域为我方国家
	    #state_trigger = { always = no }		# 针对省份的触发条件，作用域为省份，FROM作用域为敌国，FROM.FROM作用域为我方国家
	    value = 999							# 叠加到常规请求上的系数
    }


}

MGU_AI_to_PAO = {

    allowed = {
        original_tag = MGU
        has_government = communism
    }
    enable = {
        has_war_with = PAQ
    }
    abort = {
        NOT = { country_exists = PAQ }
    }

	ai_strategy = { #不要躲在雨林吃大分
		type = force_concentration_target_weight
		value = -1000
		
		state = 504
		state = 505
		state = 1204
		state = 1203
        state = 280
		state = 1205
        state = 1206
		state = 1207
        state = 1208
	}


}


#情报机构

MGU_the_more_powerful_KGB = {
    allowed = {
        original_tag = MGU
    }
    enable = {
        has_completed_focus = MTK_com_spy_16
    }

    abort = {
        always = no
    }

    ai_strategy = {
        type = intelligence_agency_usable_factories
        value = 1000
    }

    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_defense
        value = 1500
    }
    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_crypto
        value = 2000
    }

    ai_strategy = {
        type = intelligence_agency_usable_factories
        value = 20
    }

    ai_strategy = {
        type = operation_equipment_priority
        value = 100
    }

}

MGU_Willows_own_son = {

    allowed = {
        tag = MGU
        has_government = communism
    }

    enable = {
        date < 1939.6.1
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = operative_mission
        mission = build_intel_network
        value = 2500
        mission_target = AHM
    }

    ai_strategy = {
        type = operative_operation
        value = 2500
        operation = operation_infiltrate_civilian
        operation_target = AHM
    }

    ai_strategy = {
        type = operative_operation
        value = 5000
        operation = operation_steal_tech_civilian
        operation_target = AHM
    }
}

#外交

MGU_dont_mess_with_volunteers_for_no_communism_nations = { # Reverse strategy

    reversed = yes

    allowed = {
        original_tag = MGU
        has_government = communism
    }

    enable = {
        NOT = {
            has_government = communism
            has_war_with = MGU
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = support
        id = "MGU"
        value = -10
    }

    ai_strategy = {
        type = send_volunteers_desire
        id = "MGU"
        value = -10
    }

}

MGU_send_volunteers = { # Reverse strategy

    reversed = yes

    allowed = {
        original_tag = MGU
    }

    enable = {
        has_completed_focus = MTK_com_33
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = "MGU"
        value = 100
    }
}

