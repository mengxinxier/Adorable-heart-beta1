PAM_lueduo = {
    PAM_kycs_target = {
        icon = GFX_PAO_kycs_target
        visible = {
        }
        available = {
            FROM = {
                has_war_with = PAM
                casualties_inflicted_by = {
                    opponent = PAM
                    thousands > 100
                }
            }
        }
        cost = 50
        days_remove = 90
        target_trigger = {
            FROM = {
                has_war_with = PAM
            }
        }
        #修正
        modifier = {

        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        complete_effect = {
        }
        remove_effect = {
            add_command_power = 100
            add_to_variable = {
                PAM_ldzb_target = 1
            }
            custom_effect_tooltip = PAM_ldzb_target_tt
            add_to_variable = {
                PAM_ldrl_target = 1
            }
            custom_effect_tooltip = PAM_ldrl_target_tt
            add_to_variable = {
                PAM_ldgc_target = 1
            }
            custom_effect_tooltip = PAM_ldgc_target_tt
        }
        cancel_trigger = {
        }
        ai_will_do = {
            factor = 100
        }
    }
    PAM_ldzb_target = {
        icon = GFX_PAM_ldzb_target
        visible = {
        }
        available = {
            custom_trigger_tooltip = {
                tooltip =PAM_ldzb_target_t
                check_variable = { PAM_ldzb_target > 0 }
            }
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.1
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        custom_cost_text = decision_cost_CP_20
        custom_cost_trigger = {
            command_power > 20
        }
        days_remove = 7
        target_trigger = {
            has_completed_focus = PAM_zcjsck
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.1
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        remove_effect = {
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = armour_folder
                    folder = artillery_folder
                    folder = infantry_folder
                    folder = support_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_army
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_army_tt
            add_equipment_to_stockpile = {
                type = infantry_equipment
                amount = 2000
                producer = FROM
            }
            add_equipment_to_stockpile = {
                type = support_equipment
                amount = 500
                producer = FROM
            }
            FROM = {
                country_event = {
                    id = PAM.26
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }

    PAM_ldrl_target = {
        icon = GFX_PAM_ldrl_target
        visible = {
        }
        available = {
            custom_trigger_tooltip = {
                tooltip =PAM_ldrl_target_t
                check_variable = { PAM_ldrl_target > 0 }
            }
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.3
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        custom_cost_text = decision_cost_CP_20
        custom_cost_trigger = {
            command_power > 20
        }
        days_remove = 7
        target_trigger = {
            has_completed_focus = PAM_zcjsck
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.3
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        remove_effect = {
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = mtgnavalfolder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_navy
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_navy_tt
            add_manpower = 20000
            random_owned_controlled_state = {
                add_manpower = 20000
            }
            FROM = {
                country_event = {
                    id = PAM.27
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }

    PAM_ldgc_target = {
        icon = GFX_PAM_ldgc_target
        visible = {
        }
        available = {
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.5
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }

        days_remove = 7
        target_trigger = {
            has_completed_focus = PAM_zcjsck
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.5
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        fire_only_once = yes
        remove_effect = {
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = electronics_folder
                    folder = industry_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_civilian
                    target = FROM
                    uses = 1
                }
                steal_random_tech_bonus = {
                    folder = air_techs_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_airforce
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_airforce_tt
            custom_effect_tooltip = operation_steal_tech_civilian_tt
            random_owned_controlled_state = {
                limit = {
                    is_core_of = CHL
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
            random_owned_controlled_state = {
                limit = {
                    is_core_of = CHL
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
            random_owned_controlled_state = {
                limit = {
                    is_core_of = CHL
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = industrial_complex
                    level = 1
                    instant_build = yes
                }
            }
            FROM = {
                country_event = {
                    id = PAM.28
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }
}