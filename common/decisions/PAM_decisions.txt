PAM_lueduo = {
    PAM_kycs_target = {
        icon = GFX_PAM_kycs_target
        visible = {
        }
        available = {
            FROM = {
                has_war_with = PAM
                casualties_inflicted_by = {
                    opponent = PAM
                    thousands > 100
                }
            }
        }
        cost = 50
        days_remove = 15
        fire_only_once = yes
        target_trigger = {
            FROM = {
                not = {tag = ZDW}
                has_war_with = PAM
            }
        }
        #修正
        modifier = {

        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        complete_effect = {
        }
        remove_effect = {
            add_command_power = 100
            add_to_variable = {
                PAM_ldzb_target = 1
            }
            custom_effect_tooltip = PAM_ldzb_target_tt
            add_to_variable = {
                PAM_ldrl_target = 1
            }
            custom_effect_tooltip = PAM_ldrl_target_tt
            add_to_variable = {
                PAM_ldgc_target = 1
            }
            custom_effect_tooltip = PAM_ldgc_target_tt
        }
        cancel_trigger = {
        }
        ai_will_do = {
            factor = 100
        }
    }
    PAM_ldzb_target = {
        icon = GFX_PAM_ldzb_target
        visible = {
        }
        available = {
            custom_trigger_tooltip = {
                tooltip =PAM_ldzb_target_t
                check_variable = { PAM_ldzb_target > 0 }
            }
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.1
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        custom_cost_text = decision_cost_CP_20
        custom_cost_trigger = {
            command_power > 20
        }
        days_remove = 7
        target_trigger = {
            has_completed_focus = PAM_zcjsck
            FROM = {
                not = {tag = ZDW}
                has_war_with = PAM
                surrender_progress > 0.1
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        remove_effect = {
            add_to_variable = {
                PAM_ldzb_target = -1
            }
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = armour_folder
                    folder = artillery_folder
                    folder = infantry_folder
                    folder = support_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_army
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_army_tt
            add_equipment_to_stockpile = {
                type = infantry_equipment
                amount = 2000
                producer = FROM
            }
            add_equipment_to_stockpile = {
                type = support_equipment
                amount = 500
                producer = FROM
            }
            FROM = {
                country_event = {
                    id = PAM.26
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }

    PAM_ldrl_target = {
        icon = GFX_PAM_ldrl_target
        visible = {
        }
        available = {
            custom_trigger_tooltip = {
                tooltip =PAM_ldrl_target_t
                check_variable = { PAM_ldrl_target > 0 }
            }
            FROM = {
                not = {tag = ZDW}
                has_war_with = PAM
                surrender_progress > 0.3
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        custom_cost_text = decision_cost_CP_20
        custom_cost_trigger = {
            command_power > 20
        }
        days_remove = 7
        target_trigger = {
            has_completed_focus = PAM_zcjsck
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.3
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        remove_effect = {
            add_to_variable = {
                PAM_ldrl_target = -1
            }
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = mtgnavalfolder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_navy
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_navy_tt
            add_manpower = 20000
            random_owned_controlled_state = {
                add_manpower = 20000
            }
            FROM = {
                country_event = {
                    id = PAM.27
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }

    PAM_ldgc_target = {
        icon = GFX_PAM_ldgc_target
        visible = {
        }
        available = {
            custom_trigger_tooltip = {
                tooltip =PAM_ldgc_target_t
                check_variable = { PAM_ldgc_target > 0 }
            }
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.5
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }

        days_remove = 7
        target_trigger = {
            not = {tag = ZDW}
            has_completed_focus = PAM_zcjsck
            FROM = {
                has_war_with = PAM
                surrender_progress > 0.5
                any_state = {
                    is_controlled_by = PAM
                }
            }
        }
        #修正
        modifier = {
        }
        targeted_modifier = { #决议正在进行时的修正，可以专门指向某国（天数为days_remove设定的数）
        }
        fire_only_once = yes
        remove_effect = {
            add_to_variable = {
               PAM_ldgc_target = -1
            }
            hidden_effect = {
                steal_random_tech_bonus = {
                    folder = electronics_folder
                    folder = industry_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_civilian
                    target = FROM
                    uses = 1
                }
                steal_random_tech_bonus = {
                    folder = air_techs_folder
                    bonus = 2.0
                    dynamic = yes
                    name = operation_steal_tech_airforce
                    target = FROM
                    uses = 1
                }
            }
            custom_effect_tooltip = operation_steal_tech_airforce_tt
            custom_effect_tooltip = operation_steal_tech_civilian_tt
            random_owned_controlled_state = {
                limit = {
                    is_core_of = PAM
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
            random_owned_controlled_state = {
                limit = {
                    is_core_of = PAM
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
            random_owned_controlled_state = {
                limit = {
                    is_core_of = PAM
                }
                add_extra_state_shared_building_slots = 1
                add_building_construction = {
                    type = industrial_complex
                    level = 1
                    instant_build = yes
                }
            }
            FROM = {
                country_event = {
                    id = PAM.28
                }
            }
        }
        cancel_trigger = {

        }
        ai_will_do = {
            factor = 1000
        }
    }
    PAM_jingong_FROM = {
        priority = 9999
        icon = GFX_PAM_Wolf
        cost = 10
        days_remove = 15
        on_map_mode = decision_view_only
        target_trigger = {
            ROOT = {
                is_subject = no
            }
            FROM = {
                not = {
                    or = {
                        has_war_with = ROOT
                        is_in_faction_with = ROOT
                        is_subject_of = ROOT
                    }
                }
            }
        }
        allowed = {
            always = no
        }
        available = {
            FROM = {
                not = {
                    or = {
                        has_war_with = ROOT
                        is_in_faction_with = ROOT
                        is_subject_of = ROOT
                    }
                }
            }
        }
        ai_will_do = {
            base = 0
            modifier = {
                has_war = yes
                add = -100
            }
            modifier = {
                tag = MGU
                add = -100
            }
            modifier = {
                has_idea = LHS_zdwl
                add = -10000
            }
            modifier = {
                is_ai = yes
                add = -100
            }
            modifier = {
                or = {
                    has_game_rule = {
                        rule = AH_PAM_ai_behavior_SELECTION
                        option = PAO_GAME_RULE_1
                    }
                    has_game_rule = {
                        rule = AH_PAM_ai_behavior_SELECTION
                        option = PAO_GAME_RULE_2
                    }
                    has_game_rule = {
                        rule = AH_lw_war_ai_behavior_SELECTION
                        option = lw_war_GAME_RULE_3
                    }
                }
                add = 200
            }
            modifier = {
                not = {
                    has_global_flag = PAO_rebuiid
                    has_completed_focus = PAO_zhth
                }
                add = 10000
            }
        }
        complete_effect = {
            set_country_flag = PAO_war_plan_going_on
            FROM_country_is_defender_and_ROOT_is_accacker = yes
        }
        remove_effect = {
            declare_war_on = {
                target = FROM
                type = PAM_WAR
            }
            FROM = {
                every_state = {
                    limit = {
                        is_controlled_by = FROM
                        is_coastal = yes
                    }
                    damage_building = {
                        type = naval_base
                        damage = 1
                    }
                    damage_building = {
                        type = infrastructure
                        damage = 0.5
                    }
                    damage_building = {
                        type = industrial_complex
                        damage = 0.2
                    }
                    damage_building = {
                        type = arms_factory
                        damage = 0.15
                    }
                }
            }
            clr_country_flag = PAO_war_plan_going_on
        }
        cancel_trigger = {
        }
        cancel_effect = {
        }
    }
}