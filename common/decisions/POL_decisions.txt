POL_posthumous_front_movement_category = {
	
}

POL_industrial_rehabilitation_category = {

	POL_countdown_to_the_four_year_plan = {
		icon = GFX_decision_gre_paying_ifc_debt
		activation = {
			always = no
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = SOV_at_least_200_civilian_factories_tt
				num_of_controlled_factories > 199
			}
			has_completed_focus = POL_hospital_of_the_dead
		}
		fire_only_once = yes
		is_good = no
		days_mission_timeout = 1460
		modifier = {
			custom_modifier_tooltip = extra_income_of_40_per_month
		}
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { NOT = { has_country_flag = POL_remove_four_year_plan_exincome } }
					set_country_flag = POL_remove_four_year_plan_exincome
					add_to_variable = { exincome = -40 }
				}
			}
		}
		cancel_effect = {
			hidden_effect = {
				if = {
					limit = { NOT = { has_country_flag = POL_remove_four_year_plan_exincome } }
					set_country_flag = POL_remove_four_year_plan_exincome
					add_to_variable = { exincome = -40 }
				}
			}
		}
		complete_effect = {
			add_ideas = POL_casimir_industrializes
			add_offsite_building = {
				type = industrial_complex
				level = 2
			}
			add_offsite_building = {
				type = arms_factory
				level = 2
			}
			hidden_effect = {
				if = {
					limit = { NOT = { has_country_flag = POL_remove_four_year_plan_exincome } }
					set_country_flag = POL_remove_four_year_plan_exincome
					add_to_variable = { exincome = -40 }
				}
			}
		}
		timeout_effect = {
			add_political_power = -200
			add_stability = -0.20
			add_war_support = -0.20
			hidden_effect = {
				if = {
					limit = { NOT = { has_country_flag = POL_remove_four_year_plan_exincome } }
					set_country_flag = POL_remove_four_year_plan_exincome
					add_to_variable = { exincome = -40 }
				}
			}
		}
	}

	POL_duild_dec_lhydropower_station = {
		icon = GFX_decision_generic_factory
		priority = 99
		allowed = {
			always = no
		}
		available = {
			num_of_civilian_factories_available_for_projects > 4
		}
		ai_will_do = {
			factor = 10000
		}
		on_map_mode = map_and_decisions_view
		state_target = yes
		fire_only_once = yes
		days_remove = 90
		custom_cost_text = RUS_75_ubs_save_cost
		custom_cost_trigger = {
			check_variable = { ubs_save > 74 }
		}
		modifier = {
			civilian_factory_use = 5
		}
		complete_effect = {
			add_to_variable = { ubs_save = -75 }
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
				}
			}
		}
	}
	
	POL_duild_dec_thermal_station = {
		icon = GFX_decision_generic_factory
		priority = 98
		allowed = {
			always = no
		}
		available = {
			num_of_civilian_factories_available_for_projects > 4
		}
		ai_will_do = {
			factor = 10000
		}
		on_map_mode = map_and_decisions_view
		state_target = yes
		fire_only_once = yes
		days_remove = 90
		custom_cost_text = RUS_75_ubs_save_cost
		custom_cost_trigger = {
			check_variable = { ubs_save > 74 }
		}
		modifier = {
			civilian_factory_use = 5
		}
		complete_effect = {
			add_to_variable = { ubs_save = -75 }
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
				}
			}
		}
	}
		
	POL_duild_dec_manufacturing_industry = {
		icon = GFX_decision_generic_merge_plant_materiel
		priority = 97
		allowed = {
			always = no
		}
		available = {
			num_of_civilian_factories_available_for_projects > 2
		}
		ai_will_do = {
			factor = 10000
		}
		on_map_mode = map_and_decisions_view
		state_target = yes
		fire_only_once = yes
		days_remove = 90
		custom_cost_text = RUS_50_ubs_save_cost
		custom_cost_trigger = {
			check_variable = { ubs_save > 49 }
		}
		modifier = {
			civilian_factory_use = 3
		}
		complete_effect = {
			add_to_variable = { ubs_save = -50 }
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
				}
			}
		}
	}
			
	POL_duild_dec_food_factory = {
		icon = GFX_decision_generic_merge_plant_materiel
		priority = 96
		allowed = {
			always = no
		}
		available = {
			num_of_civilian_factories_available_for_projects > 2
		}
		ai_will_do = {
			factor = 10000
		}
		on_map_mode = map_and_decisions_view
		state_target = yes
		fire_only_once = yes
		days_remove = 90
		custom_cost_text = RUS_75_ubs_save_cost
		custom_cost_trigger = {
			check_variable = { ubs_save > 74 }
		}
		modifier = {
			civilian_factory_use = 3
		}
		complete_effect = {
			add_to_variable = { ubs_save = -75 }
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
				}
			}
		}
	}
	
}

POL_intermaritime_federation = {

	POL_form_intermaritime_federation = {
		priority = 99
		icon = GFX_decision_eng_propaganda_campaigns
		allowed = { always = no }
		activation = {
			always = no
		}
		available = {
			is_faction_leader = yes
			any_other_country = {
				is_in_faction_with = ROOT
			}
		}
		days_mission_timeout = 180
		fire_only_once = yes
		complete_effect = {
			add_political_power = 100
			add_stability = 0.05
			hidden_effect = {
				add_to_variable = { ubs_save = 100 }
			}
			custom_effect_tooltip = SOV_get_100_add_to_reserve
		}
		timeout_effect = {
			add_political_power = -100
			add_stability = -0.10
			add_war_support = -0.15
			leave_faction = yes
			set_country_flag = POL_intermaritime_federation_fail
			complete_national_focus = POL_identify_with_earth_defense
		}
	}

	POL_invite_dec_members = {
		icon = GFX_decision_generic_nationalism
		allowed = { always = no }
		visible = {
			is_faction_leader = yes
			is_subject = no
			FROM = { 
				is_in_faction = no
				has_government = democratic 
			}
		}
		available = {
			is_subject = no
		}
		cost = 50
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			FROM = {
				country_event = polish.28
			}
		}
	}
}

POL_territorial_affairs = {
	POL_integrate_Minskaja = {
		icon = GFX_decision_eng_trade_unions_support
		allowed = {
			tag = POL
		}
		visible = {
			ELT = { is_subject_of = ROOT }
		}
		available = {
			date > 1939.1.1
			has_completed_focus = POL_purify_polluted_areas
			ELT = { is_subject_of = ROOT }
		}
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		days_remove = 7
		fire_only_once = yes
		remove_effect = {
			ELT = { country_event = polish.012 }
			custom_effect_tooltip = if_they_accept
			effect_tooltip = {
				annex_country = {
					target = ELT
				}
			}
		}
	}
}

Project_East_Wall = {
	##向幻想乡索取援助
	POL_Ask_for_assistance_from_FRA = {
		icon = GFX_decision_generic_prepare_civil_war
		allowed = {
			tag = POL
		}
		visible = {
		}
		available = {
			always = no
		}
		cost = 15
		days_remove = 15
		days_re_enable = 120
		ai_will_do = {
			factor = 1
		}
		complete_effect = {
		}
		remove_effect = {
		}
	}
	POL_Ask_for_industry_assistance_from_FRA = {
		icon = GFX_decision_generic_factory
		allowed = {
			tag = POL
		}
		visible = {
		}
		available = {
			always = no
		}
		cost = 15
		days_remove = 15
		days_re_enable = 120
		ai_will_do = {
			factor = 1
		}
		complete_effect = {
		}
		remove_effect = {
		}
	}
	##向圣杯索取援助
	POL_Ask_for_assistance_from_ENG = {
		icon = GFX_decision_generic_prepare_civil_war
		allowed = {
			tag = POL
		}
		visible = {
		}
		available = {
			always = no
		}
		cost = 15
		days_remove = 15
		days_re_enable = 120
		ai_will_do = {
			factor = 1
		}
		complete_effect = {
		}
		remove_effect = {
		}
	}
	POL_Ask_for_industry_assistance_from_ENG = {
		icon = GFX_decision_generic_factory
		allowed = {
			tag = POL
		}
		visible = {
		}
		available = {
			always = no
		}
		cost = 15
		days_remove = 15
		days_re_enable = 120
		ai_will_do = {
			factor = 1
		}
		complete_effect = {
		}
		remove_effect = {
		}
	}
	##保卫执念
	POL_Defend_POL = {
		icon = GFX_decision_eng_blackshirt_march
		allowed = {
			tag = FRA
		}
		visible = {
			not = { has_government = communism }
		}
		available = {
			is_subject = no
			is_major = yes
			has_war_together_with = POL
			POL = {
				is_in_faction = no
				is_subject = no
				not = { has_government = communism }
			}
		}
		cost = 15
		days_remove = 7
		fire_only_once = yes
		ai_will_do = {
			factor = 1
		}
		complete_effect = {
		}
		remove_effect = {
			if = {
				limit = { is_faction_leader = yes }
				add_to_faction = POL
			}
			else = {
				faction_leader = { add_to_faction = POL }
			}
		}
	}
}

POL_purchase_grain_category = {
	POL_purchase_grain_from_dec_country = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = {
			always = no
		}
		fire_only_once = yes
		cost = 10
		days_remove = 7
		remove_effect = {
			FROM = { country_event = polish.018 }
			if = {
				limit = { FROM = { has_government = communism } }
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
			}
			else_if = {
				limit = { FROM = { has_government = fascism } }
				add_popularity = {
					ideology = fascism
					popularity = 0.05
				}
			}
		}
	}

	POL_release_new_currency_mission = {
		icon = GFX_decision_generic_welfare
		activation = {
			always = no
		}
		available = {
			always = no
		}
		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 120
		timeout_effect = {
			hidden_effect = {
				modify_timed_idea = {
					idea = POL_economic_crisis_idea
					days = -90
				}
			}
			custom_effect_tooltip = POL_economic_crisis_reduced_by_90_days
			add_ideas = POL_new_currency
		}
	}
}

POL_and_SOV_game_in_Minsk_category = {

	#第一轮：调查阶段
	POL_and_SOV_the_first_round_of_the_game = {
		icon = GFX_decision_generic_operation
		priority = 102
		is_good = no
		activation = {
			always = no
		}
		available = {
			always = no
		}
		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = PASG_cancel_trigger
				OR = {
					check_variable = { POL.POL_and_SOV_game_points > 50 }
					check_variable = { POL.POL_and_SOV_game_points < 0 }
					check_variable = { SOV.POL_and_SOV_game_points > 50 }
					check_variable = { SOV.POL_and_SOV_game_points < 0 }
				}
			}
		}
		fire_only_once = no
		days_mission_timeout = 90
		cancel_effect = {
			if = {
				limit = {
					OR = {
						check_variable = { SOV.POL_and_SOV_game_points > 50 }
						check_variable = { SOV.POL_and_SOV_game_points < 0 }
					}
				}
				hidden_effect = { 
					POL = { country_event = pol_game.005 } 
					SOV = { country_event = sov_game.006 } 
				}
			}
			if = {
				limit = {
					OR = {
						check_variable = { POL.POL_and_SOV_game_points > 50 }
						check_variable = { POL.POL_and_SOV_game_points < 0 }
					}
				}
				hidden_effect = { 
					SOV = { country_event = sov_game.005 } 
					POL = { country_event = pol_game.006 } 
				}
			}
		}
		timeout_effect = {
			if = {
				limit = {
					OR = {
						check_variable = { SOV.POL_and_SOV_game_points > 50 }
						check_variable = { SOV.POL_and_SOV_game_points < 0 }
						check_variable = { POL.POL_and_SOV_game_points > SOV.POL_and_SOV_game_points }
					}
				}
				hidden_effect = { 
					POL = { country_event = pol_game.005 } 
					SOV = { country_event = sov_game.006 } 
				}
			}
			if = {
				limit = {
					OR = {
						check_variable = { POL.POL_and_SOV_game_points > 50 }
						check_variable = { POL.POL_and_SOV_game_points < 0 }
						NOT = { check_variable = { POL.POL_and_SOV_game_points > SOV.POL_and_SOV_game_points } }
					}
				}
				hidden_effect = { 
					SOV = { country_event = sov_game.005 } 
					POL = { country_event = pol_game.006 }
				}
			}
		}
	}
	SOV_and_POL_the_first_round_of_the_game = {
		icon = GFX_decision_generic_operation
		priority = 102
		is_good = no
		activation = {
			always = no
		}
		available = {
			always = no
		}
		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = PASG_cancel_trigger
				OR = {
					check_variable = { POL.POL_and_SOV_game_points > 50 }
					check_variable = { POL.POL_and_SOV_game_points < 0 }
					check_variable = { SOV.POL_and_SOV_game_points > 50 }
					check_variable = { SOV.POL_and_SOV_game_points < 0 }
				}
			}
		}
		fire_only_once = no
		days_mission_timeout = 90
		cancel_effect = {
			
		}
		timeout_effect = {
			
		}
	}
	#第二轮：处理飞王
	POL_and_SOV_the_second_round_of_the_game = {
		icon = GFX_decision_revolt
		priority = 101
		is_good = no
		activation = {
			always = no
		}
		available = {
			always = no
		}
		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = PASG_cancel_trigger
				OR = {
					check_variable = { POL.POL_and_SOV_game_points > 50 }
					check_variable = { POL.POL_and_SOV_game_points < 0 }
					check_variable = { SOV.POL_and_SOV_game_points > 50 }
					check_variable = { SOV.POL_and_SOV_game_points < 0 }
				}
			}
		}
		fire_only_once = no
		days_mission_timeout = 60
		cancel_effect = {
			if = {
				limit = {
					NOT = { check_variable = { POL.POL_and_SOV_game_Dearche < SOV.POL_and_SOV_game_Dearche } }
				}
				add_to_variable = { POL.POL_and_SOV_game_win = 1 }
			}
			else = {
				add_to_variable = { SOV.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = {
					OR = {
						check_variable = { SOV.POL_and_SOV_game_points > 50 }
						check_variable = { SOV.POL_and_SOV_game_points < 0 }
					}
				}
				add_to_variable = { POL.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = {
					OR = {
						check_variable = { POL.POL_and_SOV_game_points > 50 }
						check_variable = { POL.POL_and_SOV_game_points < 0 }
					}
				}
				add_to_variable = { SOV.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = { check_variable = { POL.POL_and_SOV_game_win > SOV.POL_and_SOV_game_win } }
				hidden_effect = {
					POL = { country_event = pol_game.020 } 
					SOV = { country_event = sov_game.025 } 
				}
				set_global_flag = PASG_POL_win
			}
			else = {
				hidden_effect = { 
					POL = { country_event = pol_game.024 } 
					SOV = { country_event = sov_game.025 } 
				}
				set_global_flag = PASG_SOV_win
			}
			clr_country_flag = POL_and_SOV_game_in_Minsk
		}
		timeout_effect = {
			if = {
				limit = {
					NOT = { check_variable = { POL.POL_and_SOV_game_Dearche < SOV.POL_and_SOV_game_Dearche } }
				}
				add_to_variable = { POL.POL_and_SOV_game_win = 1 }
			}
			else = {
				add_to_variable = { SOV.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = {
					OR = {
						check_variable = { SOV.POL_and_SOV_game_points > 50 }
						check_variable = { SOV.POL_and_SOV_game_points < 0 }
						check_variable = { POL.POL_and_SOV_game_points > SOV.POL_and_SOV_game_points }
					}
				}
				add_to_variable = { POL.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = {
					OR = {
						check_variable = { POL.POL_and_SOV_game_points > 50 }
						check_variable = { POL.POL_and_SOV_game_points < 0 }
						NOT = { check_variable = { POL.POL_and_SOV_game_points > SOV.POL_and_SOV_game_points } }
					}
				}
				add_to_variable = { SOV.POL_and_SOV_game_win = 1 }
			}
			if = {
				limit = { check_variable = { POL.POL_and_SOV_game_win > SOV.POL_and_SOV_game_win } }
				hidden_effect = {
					POL = { country_event = pol_game.020 } 
					SOV = { country_event = sov_game.025 } 
				}
				set_global_flag = PASG_POL_win
			}
			else = {
				hidden_effect = { 
					POL = { country_event = pol_game.024 } 
					SOV = { country_event = sov_game.025 } 
				}
				set_global_flag = PASG_SOV_win
			}
			clr_country_flag = POL_and_SOV_game_in_Minsk
		}
	}
	SOV_and_POL_the_second_round_of_the_game = {
		icon = GFX_decision_revolt
		priority = 101
		is_good = no
		activation = {
			always = no
		}
		available = {
			always = no
		}
		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = PASG_cancel_trigger
				OR = {
					check_variable = { POL.POL_and_SOV_game_points > 50 }
					check_variable = { POL.POL_and_SOV_game_points < 0 }
					check_variable = { SOV.POL_and_SOV_game_points > 50 }
					check_variable = { SOV.POL_and_SOV_game_points < 0 }
				}
			}
		}
		fire_only_once = no
		days_mission_timeout = 60
		cancel_effect = {
			clr_country_flag = POL_and_SOV_game_in_Minsk
		}
		timeout_effect = {
			clr_country_flag = POL_and_SOV_game_in_Minsk
		}
	}

	PASG_enemy_is_on_the_move = {
		icon = GFX_decision_revolt
		priority = 100
		is_good = no
		activation = {
			always = no
		}
		available = {
			always = no
		}
		fire_only_once = no
		days_mission_timeout = 7
	}

	#激进策略：14天生效，禁止所有选项（14天），获得点数：7~9
	POL_and_SOV_more_aggressive_strategy = {
		icon = GFX_decision_generic_ignite_civil_war
		priority = 99
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
			OR = {
				has_active_mission = POL_and_SOV_the_first_round_of_the_game
				has_active_mission = POL_and_SOV_the_second_round_of_the_game
				has_active_mission = SOV_and_POL_the_first_round_of_the_game
				has_active_mission = SOV_and_POL_the_second_round_of_the_game
			}
		}
		available = {
			NOT = { has_country_flag = PASG_action_is_being_carried_out }
			
		}
		fire_only_once = no
		days_remove = 7
		ai_will_do = {
			base = 10
			modifier = {
				factor = 2
				check_variable = { POL_and_SOV_game_points < 10 }
			}
			modifier = {
				factor = 2
				check_variable = { POL_and_SOV_game_points < 20 }
			}
			modifier = {
				factor = 0.5
				check_variable = { POL_and_SOV_game_points > 20 }
			}
			modifier = {
				factor = 0.2
				check_variable = { POL_and_SOV_game_points > 30 }
			}
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points > 41 }
			}
		}
		complete_effect = {
			set_country_flag = PASG_action_is_being_carried_out
			hidden_effect = {
				if = { limit = { tag = POL }  SOV = { activate_mission = PASG_enemy_is_on_the_move } }
				if = { limit = { tag = SOV }  POL = { activate_mission = PASG_enemy_is_on_the_move } }
			}
		}
		remove_effect = {
			clr_country_flag = PASG_action_is_being_carried_out
			random_list = {
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 7 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 7
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 8 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 8
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 9 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 9
					}
				}
			}
		}
	}
	#稳定策略：14天生效，禁止所有选项（14天），获得点数：4~6
	POL_and_SOV_medium_strategy = {
		icon = GFX_decision_eng_blackshirt_march
		priority = 98
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
			OR = {
				has_active_mission = POL_and_SOV_the_first_round_of_the_game
				has_active_mission = POL_and_SOV_the_second_round_of_the_game
				has_active_mission = SOV_and_POL_the_first_round_of_the_game
				has_active_mission = SOV_and_POL_the_second_round_of_the_game
			}
		}
		available = {
			NOT = { has_country_flag = PASG_action_is_being_carried_out }
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points < 10 }
			}
			modifier = {
				factor = 0.5
				check_variable = { POL_and_SOV_game_points < 20 }
			}
			modifier = {
				factor = 4
				check_variable = { POL_and_SOV_game_points > 20 }
			}
			modifier = {
				factor = 0.5
				check_variable = { POL_and_SOV_game_points > 30 }
			}
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points > 44 }
			}
		}
		fire_only_once = no
		days_remove = 7
		complete_effect = {
			set_country_flag = PASG_action_is_being_carried_out
			hidden_effect = {
				if = { limit = { tag = POL }  SOV = { activate_mission = PASG_enemy_is_on_the_move } }
				if = { limit = { tag = SOV }  POL = { activate_mission = PASG_enemy_is_on_the_move } }
			}
		}
		remove_effect = {
			clr_country_flag = PASG_action_is_being_carried_out
			random_list = {
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 4 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 4
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 5 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 5
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 6 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 6
					}
				}
			}
		}
	}
	#谨慎策略：14天生效，禁止所有选项（14天），获得点数：1~3
	POL_and_SOV_cautious_strategy = {
		icon = GFX_decision_eng_propaganda_campaigns
		priority = 97
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
			OR = {
				has_active_mission = POL_and_SOV_the_first_round_of_the_game
				has_active_mission = POL_and_SOV_the_second_round_of_the_game
				has_active_mission = SOV_and_POL_the_first_round_of_the_game
				has_active_mission = SOV_and_POL_the_second_round_of_the_game
			}
		}
		available = {
			NOT = { has_country_flag = PASG_action_is_being_carried_out }
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points < 50 }
			}
			modifier = {
				factor = 2
				check_variable = { POL_and_SOV_game_points > 40 }
			}
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points > 47 }
			}
		}
		fire_only_once = no
		days_remove = 7
		complete_effect = {
			set_country_flag = PASG_action_is_being_carried_out
			hidden_effect = {
				if = { limit = { tag = POL }  SOV = { activate_mission = PASG_enemy_is_on_the_move } }
				if = { limit = { tag = SOV }  POL = { activate_mission = PASG_enemy_is_on_the_move } }
			}
		}
		remove_effect = {
			clr_country_flag = PASG_action_is_being_carried_out
			random_list = {
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 1 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 1
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 2 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 2
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_points = 3 }
					custom_effect_tooltip = {
						localization_key = PASG_increase_point
						PASGPOINT = 3
					}
				}
			}
		}
	}
	#干扰策略：14天生效，禁止所有选项（14天），降低对手点数（手上有25点时解锁）
	POL_and_SOV_interfer_with_opponents = {
		icon = GFX_decision_generic_political_discourse
		priority = 96
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
			OR = {
				has_active_mission = POL_and_SOV_the_first_round_of_the_game
				has_active_mission = POL_and_SOV_the_second_round_of_the_game
				has_active_mission = SOV_and_POL_the_first_round_of_the_game
				has_active_mission = SOV_and_POL_the_second_round_of_the_game
			}
		}
		available = {
			NOT = { has_country_flag = PASG_action_is_being_carried_out }
			custom_trigger_tooltip = {
				check_variable = { POL_and_SOV_game_points > 25 }
				tooltip = PASGPOINT_25_tt
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0.1
				check_variable = { POL_and_SOV_game_points < 30 }
			}
			modifier = {
				factor = 2
				check_variable = { POL_and_SOV_game_points > 40 }
			}
		}
		fire_only_once = no
		days_remove = 7
		complete_effect = {
			set_country_flag = PASG_action_is_being_carried_out
			hidden_effect = {
				if = { limit = { tag = POL }  SOV = { activate_mission = PASG_enemy_is_on_the_move } }
				if = { limit = { tag = SOV }  POL = { activate_mission = PASG_enemy_is_on_the_move } }
			}
		}
		remove_effect = {
			clr_country_flag = PASG_action_is_being_carried_out
			if = {
				limit = { tag = POL }
				SOV = { country_event = pol_game.026 }
			}
			if = {
				limit = { tag = SOV }
				POL = { country_event = pol_game.026 }
			}
			random_list = {
				25 = {
					if = { limit = { tag = POL } add_to_variable = { SOV.POL_and_SOV_game_points = -7 } }
					if = { limit = { tag = SOV } add_to_variable = { POL.POL_and_SOV_game_points = -7 } }
					custom_effect_tooltip = {
						localization_key = PASG_enemy_decrease_point
						PASGPOINT = 7
					}
				}
				25 = {
					if = { limit = { tag = POL } add_to_variable = { SOV.POL_and_SOV_game_points = -6 } }
					if = { limit = { tag = SOV } add_to_variable = { POL.POL_and_SOV_game_points = -6 } }
					custom_effect_tooltip = {
						localization_key = PASG_enemy_decrease_point
						PASGPOINT = 6
					}
				}
				25 = {
					if = { limit = { tag = POL } add_to_variable = { SOV.POL_and_SOV_game_points = -5 } }
					if = { limit = { tag = SOV } add_to_variable = { POL.POL_and_SOV_game_points = -5 } }
					custom_effect_tooltip = {
						localization_key = PASG_enemy_decrease_point
						PASGPOINT = 5
					}
				}
				25 = {
					if = { limit = { tag = POL } add_to_variable = { SOV.POL_and_SOV_game_points = -4 } }
					if = { limit = { tag = SOV } add_to_variable = { POL.POL_and_SOV_game_points = -4 } }
					custom_effect_tooltip = {
						localization_key = PASG_enemy_decrease_point
						PASGPOINT = 4
					}
				}
			}
		}
	}
	#撤回策略：立即生效，消耗相应点数
	POL_and_SOV_withdraw_from_the_previous_step = {
		icon = GFX_decision_hol_draw_up_staff_plans
		priority = 95
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
			OR = {
				has_active_mission = POL_and_SOV_the_first_round_of_the_game
				has_active_mission = POL_and_SOV_the_second_round_of_the_game
				has_active_mission = SOV_and_POL_the_first_round_of_the_game
				has_active_mission = SOV_and_POL_the_second_round_of_the_game
			}
		}
		available = {
			NOT = { has_country_flag = PASG_action_is_being_carried_out }
			if = {
				limit = { has_country_flag = PASG_withdraw }
				custom_trigger_tooltip = {
					has_country_flag = { flag = PASG_withdraw days > 10 }
					tooltip = POL_and_SOV_withdraw_from_the_previous_step_tooltip
				}
			}
			custom_trigger_tooltip = {
				check_variable = { POL_and_SOV_game_points > 10 }
				tooltip = PASGPOINT_10_tt
			}
		}
		fire_only_once = no
		ai_will_do = {
			base = 10
		}
		complete_effect = {
			clr_country_flag = PASG_withdraw
			set_country_flag = PASG_withdraw
			random_list = {
				25 = {
					add_to_variable = { POL_and_SOV_game_points = -7 }
					custom_effect_tooltip = {
						localization_key = PASG_decrease_point
						PASGPOINT = 7
					}
				}
				25 = {
					add_to_variable = { POL_and_SOV_game_points = -6 }
					custom_effect_tooltip = {
						localization_key = PASG_decrease_point
						PASGPOINT = 6
					}
				}
				25 = {
					add_to_variable = { POL_and_SOV_game_points = -5 }
					custom_effect_tooltip = {
						localization_key = PASG_decrease_point
						PASGPOINT = 5
					}
				}
				25 = {
					add_to_variable = { POL_and_SOV_game_points = -4 }
					custom_effect_tooltip = {
						localization_key = PASG_decrease_point
						PASGPOINT = 4
					}
				}
			}
		}
	}
	#单独计时，获得紫天组好感度
	POL_and_SOV_attract_third_parties = {
		icon = GFX_decision_hol_attract_foreign_investors
		priority = 94
		visible = {
			has_country_flag  = POL_and_SOV_game_in_Minsk
		}
		available = {
			if = {
				limit = { has_country_flag = PASG_attract_third_parties }
				custom_trigger_tooltip = {
					has_country_flag = { flag = PASG_attract_third_parties days > 14 }
					tooltip = POL_and_SOV_attract_third_parties_tooltip
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 10
				if = {
					limit = { tag = SOV }
					check_variable = { POL.POL_and_SOV_game_Dearche > SOV.POL_and_SOV_game_Dearche }
				}
				if = {
					limit = { tag = POL }
					check_variable = { POL.POL_and_SOV_game_Dearche < SOV.POL_and_SOV_game_Dearche }
				}
			}
		}
		fire_only_once = no
		cost = 50
		complete_effect = {
			clr_country_flag = PASG_attract_third_parties
			set_country_flag = PASG_attract_third_parties
			random_list = {
				33 = {
					add_to_variable = { POL_and_SOV_game_Dearche = 3 }
					custom_effect_tooltip = {
						localization_key = PASG_third_parties
						PASGTHIRDPOINT = PASGTLARGE
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_Dearche = 2 }
					custom_effect_tooltip = {
						localization_key = PASG_third_parties
						PASGTHIRDPOINT = PASGTMIDDLE
					}
				}
				33 = {
					add_to_variable = { POL_and_SOV_game_Dearche = 1 }
					custom_effect_tooltip = {
						localization_key = PASG_third_parties
						PASGTHIRDPOINT = PASGTSMALL
					}
				}
			}
		}
	}
}

POL_secondary_faction_decision_category = {

	
}