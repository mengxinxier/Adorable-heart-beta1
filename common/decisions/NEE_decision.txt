CAN_NEE_unification_compltetion_cat = {
	CAN_NEE_infrastructure_investment = {
		icon = eng_trade_unions_support

		allowed = {
			tag = CAN
		}

		available = {
			CAN = { has_completed_focus =  }
			NEE = {
				exists = yes
			}
		}

		targets = { CAN }

		target_trigger = {
			FROM = {
				tag = CAN
			}
		}

		days_remove = 10

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 0.1
			
		}

		complete_effect = {
		}

		remove_effect = {
			CAN = { 
				custom_effect_tooltip = gateway_to_europe_hol_tt
				add_to_variable = { var = can_nee_support_influence value = 5 } 
			}
			set_variable = { var = can_temp_trade_influence value = CAN.can_nee_support_influence }
			subtract_from_variable = { var = eng_temp_trade_influence value = ENG.eng_nee_support_influence }
			set_variable = { var = eng_temp_trade_influence value = ENG.eng_nee_support_influence }
			subtract_from_variable = { var = ger_temp_trade_influence value = ENG.eng_gateway_to_europe_influence }
			hidden_effect = {
				if = {
					limit = {
						CAN = {
							check_variable = { var = eng_temp_trade_influence value = 0 compare = greater_than }
							OR = {
								NOT = { has_country_flag = HOL_gateway_to_europe_britain_lead_flag }
								has_country_flag = HOL_gateway_to_europe_germany_lead_flag
							}
						}
					}
					clr_country_flag = HOL_gateway_to_europe_germany_lead_flag
					set_country_flag = HOL_gateway_to_europe_britain_lead_flag
					country_event = { id = mtg_netherlands.66 }
					GER = { country_event = { id = mtg_netherlands.66 } }
				}
				if = {
					limit = {
						HOL = {
							check_variable = { var = ger_temp_trade_influence value = 0 compare = greater_than }
							OR = {
								NOT = { has_country_flag = HOL_gateway_to_europe_germany_lead_flag }
								has_country_flag = HOL_gateway_to_europe_britain_lead_flag
							}
						}
					}
					clr_country_flag = HOL_gateway_to_europe_britain_lead_flag
					set_country_flag = HOL_gateway_to_europe_germany_lead_flag
					country_event = { id = mtg_netherlands.67 }
					ENG = { country_event = { id = mtg_netherlands.67 } }
				}
			}
		}
	}
	CAN_NEE_promote_unification = {
		
	}
	CAN_NEE_link_railroads = {
		
	}
	CAN_NEE_common_historical_education = {
		
	}
	CAN_NEE_common_historical_education = {
		
	}
	
	
	CAN_require_newengland = {
		icon = generic_nationalism
		allowed = {
			tag = CAN
		}
		available = {
			OR = {
				has_completed_focus = CAN_watch_south
				has_completed_focus = CAN_lib_south
			}
			has_war = no
		}
		cost = 150
		days_remove = 60
		fire_only_once = yes
		ai_will_do = {
			factor = 10
		}
		visible = {
			has_completed_focus = CAN_path_road
		}
		remove_effect = {
			transfer_state = 357
			transfer_state = 930
			transfer_state = 931
			transfer_state = 932
			transfer_state = 933
			transfer_state = 934
			transfer_state = 935
			transfer_state = 796
		}
	}
	
}
ENG_NEE_unification_compltetion_cat = {
	ENG_NEE_promote_integration = {
		
	}
	ENG_NEE_promote_federal_integration = {
		
	}
	ENG_NEE_financial_offensive= {
		
	}
	ENG_NEE_financial_offensive= {
		
	}
	
	